#include<bits/stdc++.h>
using namespace std;
int main(){
    string s="2+3*5/6-4";
    stack<char> opr;
    stack<int> opd;
    for(int i=0;i<s.length();i++){
            if(s[i]>=48 && s[i]<=57){
                opd.push(s[i]-48);
            }
            else{
                if(op.size()==0 ||)
            }
    }